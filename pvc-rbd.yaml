apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fio-claim-rbd
spec:
  storageClassName: ocs-storagecluster-ceph-rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
